// RobotBuilder Version: 3.1
//
// This file was generated by RobotBuilder. It contains sections of
// code that are automatically generated and assigned by robotbuilder.
// These sections will be updated in the future when you export to
// C++ from RobotBuilder. Do not put any code or make any change in
// the blocks indicating autogenerated code or it will be lost on an
// update. Deleting the comments indicating the section will prevent
// it from being updated in the future.

#include <spdlog/fmt/ostr.h>
#include <spdlog/spdlog.h>

// BEGIN AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=CONSTRUCTOR

#include "commands/VerticalConveyorRun.h"

VerticalConveyorRun::VerticalConveyorRun(int vConveyorDirection, VerticalConveyor *m_verticalconveyor) :
    m_vConveyorDirection(vConveyorDirection),
    m_verticalconveyor(m_verticalconveyor)
{
    // Use AddRequirements() here to declare subsystem dependencies
    // eg. AddRequirements(m_Subsystem);
    SetName("VerticalConveyorRun");
    AddRequirements(m_verticalconveyor);

    // END AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=CONSTRUCTOR
}

// Called just before this Command runs the first time
void VerticalConveyorRun::Initialize()
{
    spdlog::info("VerticalConveyorRun - Init {}", m_vConveyorDirection);
    m_verticalconveyor->SetVerticalConveyorSpeed(m_vConveyorDirection);
}

// Called repeatedly when this Command is scheduled to run
void VerticalConveyorRun::Execute() {}

// Make this return true when this Command no longer needs to run execute()
bool VerticalConveyorRun::IsFinished()
{
    return true;
}

// Called once after isFinished returns true
void VerticalConveyorRun::End(bool interrupted)
{
    spdlog::info("VerticalConveyorRun - End");
}

bool VerticalConveyorRun::RunsWhenDisabled() const
{
    // BEGIN AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=DISABLED
    return false;

    // END AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=DISABLED
}
